frontend: s3prl
frontend_conf:
    frontend_conf:
        upstream: codec  # Note: If the upstream is changed, please change the input_size in the preencoder.
    download_dir: ./hub
    multilayer_feature: True

type: codec